import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-weather-app-ui',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
